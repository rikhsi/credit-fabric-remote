export enum VALIDATION_ERROR {
  required = 'required',
  minlength = 'minlength',
  maxlength = 'maxlength',
  empty = 'empty',
  server = 'server',
  UserNotActive = 'UserNotActive',
  UserNotFound = 'UserNotFound',
  token = 'tokenExpired',
  email = 'email',
  loading = 'loading',
}
